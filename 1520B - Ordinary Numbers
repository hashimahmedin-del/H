t = int(input())

for i in range(t):
    n = input()          # take as string
    d = len(n)

    ans = (d - 1) * 9

    ordinary = n[0] * d   # repeat first digit

    if ordinary <= n:
        ans += int(n[0])
    else:
        ans += int(n[0]) - 1

    print(ans)